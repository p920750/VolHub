import 'package:flutter/material.dart';

class VolunteerColors {
  // 🔹 Primary brand colors (from image)
  static const Color primaryNavy = Color(0xFF0A2540);
  static const Color primaryGreen = Color(0xFF1F5E3B);

  // 🔹 Backgrounds
  static const Color background = Color(0xFFF5F6F8);
  static const Color card = Colors.white;

  // 🔹 Accent colors (stats, highlights)
  static const Color accentMint = Color(0xFFE8F7EF);
  static const Color accentSoftBlue = Color(0xFFEDF2F7);
  static const Color accentLightGreen = Color(0xFFD1FAE5);

  // 🔹 Text colors
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFF9CA3AF);

  // 🔹 Semantic
  static const Color success = Color(0xFF16A34A);
  static const Color inactive = Color(0xFF9CA3AF);
}
