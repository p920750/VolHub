import 'package:flutter/material.dart';

class AdminColors {
  // Main background color (Dark/Professional)
  static const Color background = Color(0xFFF5F7FB);
  
  // Card background
  static const Color card = Colors.white;
  
  // Primary brand color (similar to green/teal but distinct for admin)
  static const Color primary = Color(0xFF1E293B); // Dark slate
  
  // Accent color
  static const Color accent = Color(0xFF3B82F6); // Professional Blue
  
  // Status colors
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  
  // Text colors
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
}
